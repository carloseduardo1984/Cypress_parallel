{
    "scripts": {
        "test": "cypress run --browser chrome configFile=qa --record --parallel --key ${{ secrets.REC_KEY }}"
    },
    "cypress-cucumber-preprocessor": {
        "step_definitions": "cypress/support/steps"
    },
    "dependencies": {
        "cypress": "^9.7.0",
        "cypress-cucumber-preprocessor": "^4.3.1"
    }
}
